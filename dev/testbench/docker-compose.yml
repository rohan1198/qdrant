services:
  qdrant-hyperbolic:
    build:
      context: .
      dockerfile: Dockerfile.prebuilt
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage
    environment:
      - QDRANT__SERVICE__GRPC_PORT=6334

volumes:
  qdrant_storage:
